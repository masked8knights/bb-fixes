[project]
name = "bb-fixes-bootstrap"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
bb_fixes_bootstrap = "rlm.skill:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bb_fixes_bootstrap"]
